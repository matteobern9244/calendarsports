node_modules
dist
coverage
playwright-report
test-results
bun.lock
*.tsbuildinfo

# Stato locale dei plugin per agenti AI: non e' codice del progetto.
.remember

# File generati e ri-sincronizzati da upstream: la CLI Supabase per `types.ts`,
# l'agente Lovable per gli altri due, che li riscrive a ogni intervento. Non
# devono divergere per motivi di formattazione, e formattarli a mano sarebbe
# una toppa che si strappa al deploy successivo.
src/integrations/supabase/types.ts
src/integrations/supabase/client.ts
src/integrations/supabase/previewAuthStorage.ts
