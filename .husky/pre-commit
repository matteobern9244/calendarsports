# Esegue ESLint sui file .ts/.tsx in stage. La regola `no-restricted-imports`
# blocca import diretti da `@/integrations/supabase/client`: vedi AGENTS.md →
# "Import del client Supabase". In CI lo stesso check gira via `bun run lint`
# nel workflow `ci-pr-main`.
bunx lint-staged
